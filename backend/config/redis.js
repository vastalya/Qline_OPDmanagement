/**
 * Redis stub — Redis and Docker have been removed from this project.
 * All caching, queuing, and locking now uses MongoDB directly.
 * This file exports null so any legacy reference still resolves safely.
 */
module.exports = null;
